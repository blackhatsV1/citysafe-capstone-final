<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head'); %>
</head>
<body class="container">

<header>
  <%- include('../partials/header'); %>
</header>

<main>
  <div class="jumbotron mb-1">
    <h1>Welcome to CitySafe</h1>  
  </div>

  <div class="container mt-2">
    <div class="container">
      <h3 class="text-white">Recent Posts</h3><br>

      <div class="container w-60">
        <% for (let i = 0; i < users.length; i++) { %>
          <div class="mb-0 text-white">
            <strong><%= users[i].name %></strong> - posted a request.
          </div>

          <div class="container">
            <div class=" bg-light rounded-sm p-3" style="border: 1px solid black;">
              <p><strong>Location:</strong> <%= users[i].location %></p>
              <p><strong>Requesting for:</strong> <%= users[i].needs %></p>
            </div>

            <form action="/sendhelp" method="GET">
              <input type="hidden" name="name" value="<%= users[i].name %>">
              <input type="hidden" name="location" value="<%= users[i].location %>">
              <input type="hidden" name="needs" value="<%= users[i].needs %>">
              <button type="submit" class="btn btn-primary mb-5 mt-1">
                Send Help
              </button>
            </form>
          </div>

        <% } %>
      </div>

    </div>
  </div>
</main>


<footer>
  <%- include('../partials/footer'); %>
</footer>


</body>
</html>